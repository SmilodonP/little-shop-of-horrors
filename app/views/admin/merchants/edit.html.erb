<h1><%= "Update #{@merchant.name} Information" %></h1>


<%= form_with model: [:admin, @merchant], data: {turbo: false} do |form| %>
  <div>
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>
  <div>
    <%= form.submit "Submit" %>
    </div>
<% end %>
    

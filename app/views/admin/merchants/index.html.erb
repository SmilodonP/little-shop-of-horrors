<h1>Merchants</h1>

<section class="merchants_in_system">
  <% @merchants.each do |merchant| %>
    <div id="merchant-<=merchant.id%>">
      <p><%= link_to "#{merchant.name}", admin_merchant_path(merchant) %></p>
        <%= button_to "Disable", admin_merchant_path(merchant, status: :disabled), method: :patch %>
        <%= button_to "Enable", admin_merchant_path(merchant, status: :enabled), method: :patch %>
      </p>
    </div>
  <% end %>
</section>


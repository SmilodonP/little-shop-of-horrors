<h1>Merchant Dashboard</h1>

<div id="merchant">
  <h3>Name: <%= @merchant.name %></h3>
</div>

<div id="top_customers">
  <h3>Top 5 Customers by Completed Transactions:</h3>
    <% @top_five_customers.each do |customer| %>
      <li><%= "#{customer.first_name} #{customer.last_name}: #{customer.transaction_count}" %></li>
    <% end %>
</div>

<div id="merchant_links">
  <%= link_to "Items Index", merchant_items_path(@merchant) %>
  <%= link_to "Invoices Index", merchant_invoices_path(@merchant) %>
</div>

<div id="items_ready_to_ship">
  <h2>Items Ready to Ship</h2>
  <ul>
    <% @sorted_items_ready_to_ship.each do |item| %>
      <% item.invoice_items.where(status: 0).each do |invoice_item| %>
        <li>
          <%= item.name %>
          <%= link_to "Invoice #{invoice_item.invoice.id}", merchant_invoice_path(@merchant, invoice_item.invoice) %>
          <%= invoice_item.invoice.created_at.strftime('%A, %B %d, %Y') %>
        </li>
      <% end %>
    <% end %>
  </ul>
</div>